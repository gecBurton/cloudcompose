# Deprecated Code

Python files replaced by Go implementation:

- `parser.py` - Replaced by Go parser (compose-go library)
- `normalizer.py` - Replaced by Go normalizer

**Status:** Obsolete, kept for reference only

**All functionality now in:** `composey-go/` directory

**See:** `plan.md` for migration details
